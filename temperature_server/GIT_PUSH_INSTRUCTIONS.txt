# GitHub へのプッシュ - 実行手順

## 🚀 最も簡単な方法（Windows）

### ステップ 1：PowerShell を管理者として開く

1. Windows キー + R
2. `powershell` と入力
3. Ctrl + Shift + Enter で管理者として実行

### ステップ 2：以下のコマンドをコピペして実行

```powershell
cd 'i:\ESP32DS18\raspberry_pi\temperature_server'
powershell -ExecutionPolicy Bypass -File push_to_github.ps1
```

---

## 📋 手動での実行（推奨）

PowerShell または CMD を開いて、以下を順に実行してください：

```powershell
# ディレクトリに移動
cd i:\ESP32DS18\raspberry_pi\temperature_server

# Git 初期化
git init
git config user.name "Temperature Server Developer"
git config user.email "opticalbreeze@github.com"

# ファイルをステージング
git add -A

# コミットを作成
git commit -m "Initial commit: Complete dual WiFi temperature monitoring system with comprehensive documentation"

# リモートを追加
git remote add origin https://github.com/opticalbreeze/dual-wifi-temperature-monitoring.git

# メインブランチに変更
git branch -M main

# プッシュ
git push -u origin main
```

---

## ✅ 確認項目

プッシュ後、GitHub で以下を確認してください：

1. https://github.com/opticalbreeze/dual-wifi-temperature-monitoring にアクセス
2. MAIN_README.md が表示される
3. docs/ フォルダに 8 つのマークダウンファイルがある
4. コミット履歴が表示される

---

## 🆘 トラブルシューティング

### 認証エラーが出た場合

```powershell
# Windows 認証情報マネージャーをリセット
git credential reject https://github.com
```

### リポジトリが見つからないエラー

```powershell
# GitHub で以下のリポジトリが存在するか確認
# https://github.com/opticalbreeze/dual-wifi-temperature-monitoring
```

### その他のエラー

```powershell
# Git ログを確認
git log --oneline -5

# リモート状態を確認
git remote -v

# ブランチを確認
git branch -a
```
