# ラズパイへのファイル転送方法

## 🔍 接続確認手順

### 1. ラズパイのIPアドレスを確認

ラズパイには複数のネットワークインターフェースがある可能性があります：

- **WiFi APモード**: `192.168.4.1` (RaspberryPi_Temperature に接続)
- **WiFi クライアントモード**: `192.168.11.x` または `192.168.1.x`
- **有線LAN**: 設定による

### 2. 接続確認コマンド

```powershell
# ラズパイのAPに接続している場合
ping 192.168.4.1

# 通常のWiFiネットワーク経由の場合
ping 192.168.1.93
ping 192.168.11.57

# ネットワークスキャン（arp-scanが使える場合）
arp -a | Select-String "raspberry"
```

## 📤 転送方法

### 方法1: WiFi AP経由で接続（推奨）

ラズパイのWiFi AP（RaspberryPi_Temperature）に接続してから転送：

```powershell
# 1. WiFi APに接続（SSID: RaspberryPi_Temperature）
# 2. 接続後、以下を実行
$REMOTE_HOST = "raspberry@192.168.4.1"
$REMOTE_DIR = "/home/raspberry/temperature_monitoring/temperature_server"

scp temperature_server/templates/dashboard.html "${REMOTE_HOST}:${REMOTE_DIR}/templates/dashboard.html"
scp temperature_server/templates/stream.html "${REMOTE_HOST}:${REMOTE_DIR}/templates/stream.html"
scp temperature_server/app/routes/api.py "${REMOTE_HOST}:${REMOTE_DIR}/app/routes/api.py"
scp temperature_server/database/queries.py "${REMOTE_HOST}:${REMOTE_DIR}/database/queries.py"
```

### 方法2: USBメモリ経由

1. 変更した4つのファイルをUSBメモリにコピー
2. USBメモリをラズパイに接続
3. ラズパイで以下を実行：

```bash
# USBメモリがマウントされている場所を確認
lsblk
# 通常は /media/raspberry/ 以下

# ファイルをコピー
cp /media/raspberry/temperature_server/templates/dashboard.html /home/raspberry/temperature_monitoring/temperature_server/templates/
cp /media/raspberry/temperature_server/templates/stream.html /home/raspberry/temperature_monitoring/temperature_server/templates/
cp /media/raspberry/temperature_server/app/routes/api.py /home/raspberry/temperature_monitoring/temperature_server/app/routes/
cp /media/raspberry/temperature_server/database/queries.py /home/raspberry/temperature_monitoring/temperature_server/database/
```

### 方法3: 直接編集（SSH接続できる場合）

ラズパイにSSH接続して、直接ファイルを編集：

```bash
ssh raspberry@192.168.4.1
# または
ssh raspberry@192.168.1.93
```

### 方法4: Webインターフェース経由（管理画面から）

ラズパイの管理画面にアクセスできる場合、ファイルを直接編集可能な場合があります。

## 🔧 サーバー再起動（転送後）

ファイル転送後、サーバーを再起動：

```bash
ssh raspberry@[IPアドレス] "cd /home/raspberry/temperature_monitoring/temperature_server && pkill -f 'python.*run.py' && sleep 2 && source venv/bin/activate && nohup python run.py > server.log 2>&1 &"
```

## 📋 転送するファイル一覧

以下の4つのファイルを転送してください：

1. `temperature_server/templates/dashboard.html`
2. `temperature_server/templates/stream.html`
3. `temperature_server/app/routes/api.py`
4. `temperature_server/database/queries.py`

